import { ElementId } from '../../../common'
import * as React from 'react'

export const About: React.FunctionComponent = () => (
    <div id={ElementId.About}>Anything you need here for about info</div>
)
